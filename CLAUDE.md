# artposter

강의·교육용 웹 도구 모음. 각 도구는 의존성 없는 단일 HTML 파일로 만든다.

## 산출물 브랜딩 (필수)

**이 저장소에서 만드는 모든 산출물에는 제작자 마크 `Mindstream LAB · Awesome JWY`를 넣는다.**
웹앱, 슬라이드(PPTX), 문서(DOCX/PDF), 이미지, 포스터 등 결과물 형태를 가리지 않는다.

- 자산과 사용법: `assets/brand/README.md`
- 웹 페이지 → 푸터에 가로 1줄 마크. 단일 파일 배포면 SVG를 문서에 직접 인라인한다.
- 슬라이드·문서 → 표지에 세로형, 본문 하단에 가로형 PNG.
- 배경이 어두우면 `-dark`, 밝으면 `-light` 변형을 쓴다.
- 마크를 빠뜨린 산출물은 미완성으로 본다.

## 파일

| 파일 | 내용 |
|---|---|
| `index.html` | Wanderlust 바우하우스 포스터 메이커 |
| `pomodoro.html` | NIA 강의 타이머 (3·5·10분) |
| `assets/brand/` | 제작자 브랜드 마크 자산 |
| `assets/nia-ci.png` | 기관 CI 이미지를 넣는 자리 (없으면 텍스트 마크로 폴백) |

## 코드 규칙

- 외부 라이브러리 의존을 늘리지 않는다. 기존 파일이 쓰는 CDN 범위 안에서 해결한다.
- 사용자 입력은 `textContent`로 렌더링한다 (`innerHTML` 금지).
- 타이머류는 `setInterval` 누적 오차를 피해 종료 시각(`endAt`) 기준으로 남은 시간을 계산한다.
- 저장은 `localStorage`, 실패해도 앱이 죽지 않도록 `try/catch`로 감싼다.
- 주석·UI 문구는 한국어를 기본으로 한다.
